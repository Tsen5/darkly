In the robots.txt file, we can find a path to a file with a md5 encoded root password 
(http://<ip>/whatever/htpasswd). 
When we decode the password, we get a combinaison root:dragon, that can be use in admin login page.

URL: http://<ip>/admin/
